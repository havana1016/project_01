<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="fr">
<insert id="save">
    insert into ftable (mid,fid,ffname) values (#{mid},#{fid},#{ffname})
</insert>
    <select id="findall" resultType="fdto">
        select * from ftable where mid=#{mid} order by mtime desc
    </select>
    <update id="mtime">
        update ftable set mtime=#{mtime} where mid=#{mset} and fid=#{mget} or mid=#{mget} and fid=#{mset}
    </update>

</mapper>














